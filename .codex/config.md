## Task Rules

Default mode: Local change.

Assume the requested change is isolated.

Never inspect the project unless evidence requires it.

Always explain why another file is needed before reading it.

Stop after solving the requested problem.